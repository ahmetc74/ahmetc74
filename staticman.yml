comments:
  branch: master
  path: "_data/comments/{options.slug}"
  requiredFields: ['name', 'email', 'message']
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
